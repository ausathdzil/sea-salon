export type Reviews = {
  id: number;
  name: string;
  rating: number;
  comment: string;
};

export type Reservations = {
  id: number;
  name: string;
  phone_number: string;
  service: string;
  date: string;
  time: string;
};
